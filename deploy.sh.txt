#!/bin/bash

echo "🚀 DÉPLOIEMENT VERS GITHUB & RENDER"
echo "===================================="

# Demander les informations
read -p "👤 Votre nom d'utilisateur GitHub : " GADIAGA-prog
read -p "🔗 Nom du dépôt (ex: mon-site) : " parispromax

# Configurer Git
git config --global user.name "$username"
git config --global user.email "$username@gmail.com"

# Initialiser et push
git init
git add .
git commit -m "Premier déploiement $(date)"
git branch -M main
git remote add origin "https://github.com/$username/$repo.git"
git push -u origin main

echo ""
echo "✅ Terminé ! Rendez-vous sur :"
echo "📦 GitHub : https://github.com/$username/$repo"
echo "🚀 Render : https://$repo.onrender.com"