artifacts/
data/
*.joblib
predictions.json
__pycache__/
*.pyc
.mlvenv/

# Note: the trained model (model/model.cbm) IS committed so the Render IA image
# embeds it. Retrain locally then `git add -f ml/model/model.cbm`.
